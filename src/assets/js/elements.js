import CountClicks from './countClicks';
import CountFlag from './countFlags';
// import GameHeader from './gameHeader';
import Timer from './timer';

export default {
  // header: new GameHeader(),
  counterFlag: new CountFlag(),
  counterClick: new CountClicks(),
  timer: new Timer(),
};
